<Window x:Class="Kirby_New_Adventure.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kirby_New_Adventure"
        mc:Ignorable="d"
                Title="Kirby el asesino de dioses" Height="500" Width="800"
       MinWidth="300" MinHeight="300"
Background="{StaticResource BackgroundColor}"
Foreground="{StaticResource TextColor}"
FontFamily="{StaticResource MainFont}"
WindowStartupLocation="CenterScreen"
       
         KeyDown="Window_KeyDown">
    <!--PreviewKeyDown="Window_PreviewKeyDown"-->

    <Grid>
        <Viewbox>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>

                </Grid.RowDefinitions>
                <TextBlock x:Name="ScoreText" HorizontalAlignment="Center" VerticalAlignment="Center" Text="SCORE 0"
   FontSize="22" Margin="10"></TextBlock>

                <Border x:Name="GridBorder" Grid.Row="1" BorderBrush="{StaticResource GridLineColor}" BorderThickness="1.5"
HorizontalAlignment="Center" VerticalAlignment="Center" RenderOptions.EdgeMode="Aliased" Margin="20 0 20 20">


                    <UniformGrid x:Name="GameGrid" Width="400" Height="400" Background="{StaticResource GridBackgroundColor}"
                SnapsToDevicePixels="True" RenderOptions.BitmapScalingMode="HighQuality">

                    </UniformGrid>
                </Border>

            </Grid>

        </Viewbox>
        <Border x:Name="Screnganar" Grid.Row="1" Background="Aquamarine" Visibility="Hidden">
            <Label FontSize="40">Ganaste</Label>
        </Border>

        <Border x:Name="SelectNivel" Background="LightPink">
            <Grid x:Name="LevelScreen" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock FontSize="59" Foreground="Black" HorizontalAlignment="Center">Escoge la dificultad</TextBlock>

                <UniformGrid Columns="3" Rows="2" Grid.Row="1" VerticalAlignment="Center" Width="300">
                    <UniformGrid.Resources>
                        <Style TargetType="RadioButton">
                            <Setter Property="FontSize" Value="30"/>
                        </Style>
                    </UniformGrid.Resources>
                    <RadioButton x:Name="btnfacil" GroupName="difi">Facil</RadioButton>
                    <RadioButton x:Name="btnnormal" GroupName="difi" IsChecked="True">Normal</RadioButton>
                    <RadioButton x:Name="btndificil" GroupName="difi" >Dificil</RadioButton>
                    <Button Grid.Row="1" Grid.Column="1" Height="60" Width="100" FontSize="30" x:Name="Jugar" Click="Jugar_Click">Jugar</Button>
                </UniformGrid>
                
                


            </Grid>
        </Border>
        <Border x:Name="Overlay" Grid.Row="1" Background="Red"  
RenderOptions.EdgeMode="Aliased">

            <Grid x:Name="LockScreen" >
                <TextBlock FontSize="50">Kirby New Adventure alv</TextBlock>
                <Button Width="70" Height="50" FontSize="30" x:Name="btnInicio" Click="btnInicio_Click">Start</Button>

            </Grid>


        </Border>

    </Grid>
    
</Window>
